#include <stdio.h>
void leer_matriz(e[3][2]);
int calcular_maximo(p[3][2]);

int main(){
	int matriz[3][2];
	leer_matriz(matriz);
	int resultado = calcular_maximo(m);
		printf("El valor maximo de la primera columna es: %d", calcular_maximo);
	return 0;
}

void leer_matriz(e[3][2]){
	printf("Introduce los valores de las filas: \n");
		for(int i = 0; i < 3; i++){
			for(int j = 0; j < 3; j++){
			scanf("%d", &e[i][j]);
		    }
		}
}

int calcular_maximo(p[3][2]);
	int maximo1 = p[0][0];
		for(int i = 0; i < 3; i++){
			if (maximo1<p[i][0]){
				maximo1 = p[i][0]
			}
		return (maximo);
}

